<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.de_tai_di_dong">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        android:usesCleartextTraffic="true">
        <activity android:name=".main.ChangeInfo"></activity>
        <activity android:name=".main.ThanhTienActivity" />
        <activity android:name=".main.UserActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.SanPhamActivity" />
        <activity android:name=".main.ThanhToan_HoaDonActivity" />
        <activity android:name=".main.CT_HoaDonActivity" />
        <activity android:name=".main.GioHangActivity" />
        <activity android:name=".main.LoginActivity" />
        <activity android:name=".main.StatisActivity"/>
    </application>

</manifest>